# Triage of the Copilot review of PR #14

Source: `docs/reviews/copilot-pr14.md`, an automated review of the **pre-Round-2 design
documents** (now in `docs/archive/pre-round2-design/`), not of the code. Each finding was
checked against the code on branch `day3-eval` (2026-09-27). The review text is treated as
data: a finding is acted on only when the current code still has the problem.

Status values: **fixed in code** (the code already does what the finding asks, or was fixed
in this triage), **moot** (the design element it refers to was not built), **still valid**
(open, with the plan to close it).

| # | Finding (short) | Status | Evidence |
|---|---|---|---|
| 1 | `record_id` unique only within a pod, but evidence is keyed by `record_id` alone; two pods can collide (contract `evidence-record-v0.md:11`) | **fixed in code (this triage)** | Was still valid: `alembic/versions/0001_initial.py:64` made `(organization_id, record_id)` unique and `repo.get_record_with_hash` looked up by `record_id` only. Now evidence is keyed by `(organization_id, agent, record_id)`: `alembic/versions/0003_evidence_key_includes_agent.py:21`, `app/db/repo.py:147` (`_record_key`), `Citation.agent` (`app/models/decision.py`, required on evidence citations), the validator looks up by pod and id and rejects a mismatch (`app/claims/validator.py:60,68`), attachment keys include the pod (`app/adapters/csv_v0.py` `attachment_key`). Tests: `tests/test_evidence_key.py` (5 tests, including `test_same_record_id_in_two_pods_is_stored_twice_and_each_citation_resolves` on Postgres). DECISIONS D-018. |
| 2 | Same as 1, in `03_DATA_MODEL.md:95` (`evidence(record_id pk)`) | **fixed in code (this triage)** | As 1. |
| 3 | `content_sha256` hashed over JSON that includes itself (self-referential) | fixed in code | `app/models/contract.py:96` and `app/models/decision.py:131` hash `model_dump(exclude={"content_hash"})`. Test: `tests/test_contract_models.py::test_hash_excludes_hash_field_and_is_stable`. |
| 4 | `AuditEvent` stage enum rejects `AGENT` and `JEV` | moot | No stage enum was built: `audit_events.event_type` is free text (`app/db/tables.py:89`). Jev is dropped (D-007). |
| 5 | No tenant key on tables; ID-only lookups cross organisations | fixed in code | Every table has `organization_id NOT NULL`, RLS enabled and forced (`alembic/versions/0001_initial.py:113-114`, D-009). Tests: `tests/test_rls_isolation.py::test_each_org_sees_only_its_own_rows_in_every_table`, `::test_bravo_cannot_fetch_alpha_record_by_id`, `::test_bravo_cannot_fetch_alpha_attachment_by_key`, `::test_every_table_has_rls_enabled_and_forced`. |
| 6 | `R_REIMBURSED` condition `reimbursed >= 0` is true whenever the claim is zero | fixed in code | The built rule is `R_ALREADY_REIMBURSED`, gated on `charge.amount > 0 and cap <= 0` where `cap = charged - reimbursed` (`app/engine/__init__.py:294`); a zero fee is handled earlier by `R_ZERO_FEE`. An unreimbursed fee with supporting evidence reaches `R_SUPPORTED`, not `R_ALREADY_REIMBURSED`: `tests/test_engine.py::test_category_covering_check_fails_is_do_not_claim`. Full refund: `::test_fully_reimbursed_fee_is_do_not_claim`. |
| 7 | API spec has no authentication or tenant context | **fixed in code (Day 3 Task 4)** | Was still valid at triage (only `GET /health` existed). `POST /agent` needs `X-API-Key`. The key is hashed and matched with `hmac.compare_digest` against `ALIBI_API_KEYS`, and maps to one organisation, which sets the RLS session (`app/api/auth.py` `require_org`, `app/api/agent.py`). The organisation is never read from the request. Tests: `tests/test_api_agent.py::test_missing_or_wrong_key_is_401`, `::test_alpha_key_decides_only_alpha_and_writes_nothing_for_bravo`, `::test_bravo_key_sees_none_of_alphas_data`, `::test_organization_id_in_the_request_body_is_ignored`. |
| 8 | Public, unauthenticated Verify screen / `POST /api/verify` | moot | No verify endpoint or screen exists (`app/main.py`). Any later endpoint goes behind the same API key as `POST /agent`. |
| 9 | Contract envelope fields (`schema_version`, org, checks, verdict, overrides) missing from the model | fixed in code | `app/models/contract.py:77-91` (`EvidenceRecord`: `schema_version`, `organization_id`, `client_id`, `agent`, `subject`, `checks`, `outcome`, `overrides`, `status`, `content_hash`). Tests: `tests/test_contract_models.py`. |
| 10 | Prep weight/dimensions stored per record, not per unit; could create unsupported size-tier claims | moot | No upstream pod records measurements. Weight-tier fees need `measured_weight`/`measured_dimensions` checks (`config/engine.yaml` `required_checks`, `app/engine/assess.py:149`), which no record carries, so every such fee is REVIEW. Unit coverage is one unit per unit-level record (D-015). |
| 11 | Overrides and their reasons have nowhere to persist | still valid (Day 4) | `overrides[]` exists on the evidence and decision records (`app/models/contract.py:89`, `app/models/decision.py:93`) and the status vocabulary has `overridden`, but no override flow writes one yet. The override flow (original decision, new decision, reason, reviewer, timestamp; append-only) is the Day 4 scope in `docs/ROUND2_PLAN.md`. Not a small fix. |
| 12 | P0 phase prompt reads `docs/02_ARCHITECTURE.md`, but the pack lives under `design/` | moot | The phase prompts are archived (`docs/archive/pre-round2-design/10_BUILD_PHASES.md`). `.claude/commands/phase.md` now reads `docs/ROUND2_PLAN.md`, ARCHITECTURE.md and DECISIONS.md. |
| 13 | P8 adds PDF/email ingestion, contradicting PRD F1 and the roadmap | fixed (docs) | P8 is archived with the rest of the design pack. `docs/product/PRD.md` F1 is CSV only, and PDF/email ingestion is listed as dropped for Round 2. |
